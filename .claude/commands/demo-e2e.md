---
description: Run the scripted API demo storyline with assertions
---
Ensure the DB is up and seeded (`npm run db:up`; if tables are missing run `npm run db:reset`). Run `npm run demo:e2e`. Summarize each storyline step as PASS/FAIL with the failing assertion text. Do not modify source; if it fails, diagnose the root cause and propose the fix.
